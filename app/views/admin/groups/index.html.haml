%h1 Listing admin_groups

%table
  %tr
    %th
    %th
    %th

  - @admin_groups.each do |admin_group|
    %tr
      %td= link_to 'Show', admin_group
      %td= link_to 'Edit', edit_admin_group_path(admin_group)
      %td= link_to 'Destroy', admin_group, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Group', new_admin_group_path
